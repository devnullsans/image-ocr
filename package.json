{
  "name": "image-ocr",
  "version": "1.0.0",
  "description": "ocr images to text using google vision",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "test": "mv public/.gitignore .publicgitignore && rm -r public && mkdir public && mv .publicgitignore public/.gitignore && cd react-client && npm run build && mv ./build/* ../public/ && cd .. && node server.js"
  },
  "keywords": [],
  "author": "devnullsans",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.1",
    "node-fetch": "^3.2.10"
  }
}
